namespace ShareX.Avalonia.UI.ViewModels
{
    /// <summary>
    /// Editor tool types for annotation
    /// </summary>
    public enum EditorTool
    {
        Select,
        Rectangle,
        Ellipse,
        Arrow,
        Line,
        Text,
        Number,
        Spotlight,
        Crop
    }
}
